# Big-Screen Dashboard Verification

- render the home screen at the target viewport and verify scale, stage alignment, and interaction hotspots
- verify the affected chart, dialog, warning, or scene path after resize and remount
- verify the relevant domain API contract or service mapping
- verify at least one management subpage if shared shell or service logic changed
- verify any terrain, material, or coordinate-dependent artifact assumptions when scene data changed
