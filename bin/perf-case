#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
TOOL_ROOT="$ROOT_DIR/tooling/perf-diagnostics"

PYTHONPATH="$TOOL_ROOT/src:$ROOT_DIR/tooling${PYTHONPATH:+:$PYTHONPATH}" \
  exec "$ROOT_DIR/bin/python.sh" -m perf_diagnostics.cli "$@"
