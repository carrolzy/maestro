#!/bin/bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
TOOL_ROOT="$SCRIPT_DIR/../tooling/perf-diagnostics"
SYSTEM_TOOLING_ROOT="$SCRIPT_DIR/../tooling"

PYTHONPATH="$TOOL_ROOT/src:$SYSTEM_TOOLING_ROOT" python3 -m perf_diagnostics.cli "$@"
